navigator.geolocation.getCurrentPosition(position => {

});

const currentPosition = {lat : positionInfo.coords.latitude, lng : positionInfo.coords.longitude}




const currentStateUpdate = (state = currentState, action) => {

}
